<script type="text/javascript">
  $(document).ready(function(){
      $("."+ $("#active_class").val()).addClass("active");
        // $(".active_class").removeClass("active");
        // $(this).addClass("active");
  });
</script>
<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><img src="https://www.google.com.pk/images/srpr/logo11w.png"></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <% if current_user.role == 'client' %>
        <li class="invitations"><a href="<%= users_path %>">List users</a></li>
        <li class="invite_user"><a href="<%= new_user_path %>">Add user</a></li>

        
        
      <% elsif current_user.role == "admin" %>
        <li class="invitations"><a href="<%= users_path %>">List carriers</a></li>
        <li class="invite_user"><a href="<%= new_user_path %>">Add carriers</a></li>
      <% end %>
      <% unless current_user.role == 'admin' %>
        <li class="invitations"><a href="<%= customers_path %>">Existing customers</a></li>
        <li class="invite_user"><a href="<%= new_customer_path %>">Add customer</a></li>
        <li class="invite_user"><a href="<%= closedCustomers_customers_path %>">Cloased customers</a></li>
      <% end %>   
        
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to  current_user.email, edit_user_path(current_user) %></li>
          <li><%#= link_to 'Destroy', project, data: { confirm: 'Are you sure?' }, method: :delete, class: "btn btn-sm btn-danger" %>
          <a href="<%= destroy_user_session_path %>" data-method="delete">Log out</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
